# AI Memory Vault v3 — 環境變數
# 密鑰類設定請放此處，不要寫入 config.json。
# config.json 的 llm.api_key_env 欄位指定要讀取的環境變數名稱。

# Google Gemini API Key（若 LLM provider 選 gemini）
# GOOGLE_API_KEY=your-google-api-key

# GitHub Token（若未來整合 Copilot SDK）
# GITHUB_TOKEN=your-github-token

# Debug 模式
DEBUG=false
