---
project: ai-memory-vault
org: lifeofdevelopment
date: 2026-04-08
type: daily
---

# AI Memory Vault — 2026-04-08 每日進度

## 今日完成

### Phase 16：MCP 工具回傳字串中文化
- `mcp_app/tools/vault_tools.py` — 9 處英文 return 字串全改繁體中文
- `mcp_app/tools/scheduler_tools.py` — 8 處英文字串改中文
- `mcp_app/tools/project_tools.py` — Error/Knowledge card 改中文
- `mcp_app/tools/todo_tools.py` — no match / updated / added 改中文
- `mcp_app/tools/index_tools.py` — 8 處 DB 狀態、清理、重建訊息改中文
- `mcp_app/tools/agent_tools.py` — Agent not found / domain / skills 改中文
- `Vault/_config/agents.md` — 新增 `## 🌐 語言規定` 區段

### VS Code Prompts 架構重整（唯一橋接入口原則）
- **新建** `vault-bridge.instructions.md` — 合併舊的兩個橋接檔，精簡為最小集合
- **刪除** `vault-coding-rules.instructions.md`
- **刪除** `VaultWriteConventions.instructions.md`
- `agents.md` frontmatter 加 `inject: true` → MCP 連線時自動注入語言規定
- `global-prompts-maintenance.instructions.md` — 加入分層架構原則表格

### 排程任務名稱 Bug 修正
- **Bug**：新增排程時允許輸入任意名稱，但查詢過濾條件寫死為 `TaskName -match "AI-MemoryVault"`，導致自訂名稱任務建立成功但查不到
- **修正**：`vault-menu.ps1` 改為只輸入後綴，`AI-MemoryVault-` 前綴自動加上，並防止重複前綴
- **清理**：刪除測試殘留任務 `Test Daily`、`Test`（共 2 個）
- **重建**：`AI-Memory-Vault-Setup-v3.5.0.exe`（301.6 MB，06:22）

## 關鍵決策

- **原則確立**：`vault-bridge.instructions.md` = 語言規定 + 去哪裡找的指令（最小集合）；實質內容全在 Vault（`agents.md`、`nav.md`、`rules/*.md`）
- 未來新增指示 → 寫入 Vault，`vault-bridge.instructions.md` 不動
- 排程任務名稱強制前綴 `AI-MemoryVault-`，確保查詢可找到

## 遇到的問題

- 初版 vault-bridge 仍複製了 Vault 內容 → 第二次精簡後移除
- 排程新增顯示成功但查詢看不到 → 名稱前綴不一致導致過濾失敗

## 明日計畫

- 重新安裝測試（`AI-Memory-Vault-Setup-v3.5.0.exe`）
- 新增排程任務驗證（前綴格式是否正確）
- 確認 34 個 MCP 工具在 VS Code Copilot Chat 均可正常呼叫

## 今日學到

- VS Code `.instructions.md` 的 `applyTo: "**"` 是全域的，不需要 `.vscode/copilot-instructions.md` 補刀
- `inject: true` 只影響 MCP client 連線時的 system prompt，跟 VS Code `.instructions.md` 是兩條平行路徑
- Windows Task Scheduler 的查詢是完全字串比對，任意名稱 + regex 過濾要一起設計好
