---
type: daily
project: ai-memory-vault
org: LIFEOFDEVELOPMENT
date: 2026-04-06
phase: 28
---

# 2026-04-06 每日進度

## 今日完成

### Phase 28 — 打包就緒修復

**pyproject.toml 修復：**
- `dependencies = []` 填入 20 個依賴（與 requirements.txt 完整同步）
- 移除無效 `[project.scripts] vault = "main:main"`（main.py 是腳本，非 package module）
- 新增 `[tool.setuptools]` 明確列出 5 個 package（core / services / mcp_app / cli / tools）
- 新增 `[tool.setuptools.exclude-package-data]` 排除平台腳本（*.ps1/*.bat）、測試腳本、設定檔

**其他修復：**
- `tools/__init__.py` 建立（匯出 TOOL_REGISTRY / REGISTRY_BY_NAME / REGISTRY_BY_ALIAS）
- `.gitignore` 補 `AI_Engine/*.bak` 和 `.pytest_cache/`
- 空 `docs/` 目錄移除

**分發架構釐清：**
- pip install (wheel)：只含 5 個 Python package，排除平台腳本
- git clone：完整，平台腳本保留原位（依賴同目錄引用，移動会壞）
- 排程功能：pip 用 `python main.py --scheduler`；git clone 用 `.bat`/`.ps1` 註冊 Windows 工作排程器

**測試狀態：101/101 PASS**

### 路線圖更新

評估三種分發方案時程，加入 README 與 status.md：

| Phase | 方案 | 預計工時 | 說明 |
|-------|------|---------|------|
| Phase 4 | PyInstaller → `.exe` | 0.5~1 天 | 雙擊即用，非技術人員可用 |
| Phase 5 | Inno Setup 安裝精靈 | 0.5 天 | 含桌面捷徑，依賴 Phase 4 |
| Phase 6 | Tauri + React GUI | 3~4 週 | 完整桌面 App，長期目標 |

## 遇到的問題

- `tools/` 缺 `__init__.py`：Python 3.3+ namespace packages 可正常 import，但 setuptools 打包需要明確 `__init__.py`
- `pyproject.toml [project.scripts]` 的 `"main:main"` 格式要求 `main` 是 installed package 的模組路徑，但 `main.py` 放在腳本根目錄不在任何 package 內，移除較乾淨

## 明日計畫

- 開始 **Phase 4：PyInstaller 打包**
  - 安裝 PyInstaller：`pip install pyinstaller`
  - 建立 `build.spec`（三個 entry point：cli / mcp / scheduler）
  - 建立 `build.ps1` 一鍵打包腳本
  - 測試 `.exe` 能正常啟動 CLI 和 MCP 模式

## 學到的事

- `pyproject.toml [tool.setuptools] packages` 明確列出比 `find_packages()` 更可控，特別是有平台腳本混在根目錄時
- PyInstaller 適合「零安裝」分發，但無法跨平台（Windows 只能打 Windows exe）
- 排程功能的分發策略：Python 層（APScheduler daemon）跨平台；OS 層（Windows 工作排程器）平台限定
