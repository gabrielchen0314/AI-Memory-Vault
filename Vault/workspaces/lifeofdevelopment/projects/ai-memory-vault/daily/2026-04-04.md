---
type: project-daily
date: 2026-04-04
project: ai-memory-vault
org: LIFEOFDEVELOPMENT
phase: 18
---

# 2026-04-04 — Phase 15–18 開發日誌

## 完成項目

### 新增規則架構（3 層）
- 建立通用規則：`workspaces/_global/rules/01-coding-style-universal.md`
  - 命名前綴（m_/i_/_ 對應 C#/Lua/Python）
  - 結構順序、格式、三語言 XML doc 模板、Git commit type、安全規則
- 建立 `Chinesegamer/rules/01-csharp-coding-style.md`（C# Unity 專屬）
- 建立 `Chinesegamer/rules/02-lua-coding-style.md`（Lua xLua 專屬）
- 建立 `lifeofdevelopment/rules/01-python-coding-style.md`（Python 專屬）
- 更新 `_config/agents.md`：加入 Coding 規則索引 section

### Phase 15 E2E 測試（26 tests, all passing）
- 先驗基準：14/14 tests for Steps 1–6
  - `write_note`, `search_vault`, `read_note`, `generate_project_status`,
    `list_projects`, `sync_vault`
- 修復：初始化順序 `vectorstore.initialize()` 必須先於 `VaultService.initialize()`
- 修復：PowerShell CP950 不支援 emoji → 改用 `[PASS]`/`[FAIL]` ASCII 標記
- 擴充至 26 tests，Steps 7+8 通過

### batch_write_notes 工具開發
- `core/indexer.py`：新增 `sync_batch(iAbsPaths: list) -> dict`
  - N 個檔案 → 1 次 `langchain_index()` 呼叫（原本是每檔一次）
- `services/vault.py`：新增 `batch_write_notes(iNotes: list) -> tuple`
  - 先寫入所有檔案，最後單次 `sync_batch()` 批量索引
- `mcp_app/server.py`：暴露為 MCP 工具

### update_todo 工具開發
- `services/vault.py`：新增 `update_todo(iFilePath, iTodoText, iDone) -> tuple`
  - 以部分文字比對找到目標行
  - 切換 `[ ]` ↔ `[x]`，只修改 checkbox，不覆蓋全文
  - 寫回後呼叫 `sync_single()` 更新索引
- `mcp_app/server.py`：暴露為 MCP 工具

### 規則：新功能 DB 更新檢查
- 確認 `generate_project_status` 呼叫 `_sync_write()` → ChromaDB ✅
- `list_projects` 為純讀操作，無需 DB 更新 ✅
- 規則確立：所有新寫入型工具必須有對應 DB 更新路徑，不重複接口

## 遇到問題與解法

| 問題 | 原因 | 解法 |
|------|------|------|
| `ModuleNotFoundError: langchain_huggingface` | 用系統 Python | 改用 `.venv\Scripts\python.exe` |
| `RuntimeError: VaultService not initialized` | 初始化順序錯誤 | `vectorstore.initialize()` 先呼叫 |
| `UnicodeEncodeError: 'cp950'` | 終端機不支援 emoji | 改用 ASCII `[PASS]`/`[FAIL]` |

## 最終 E2E 結果
```
=== Phase 15 E2E Test ===
Step 1: write_note           → 3 PASS
Step 2: search_vault         → 2 PASS
Step 3: read_note            → 2 PASS
Step 4: generate_project_status → 3 PASS
Step 5: list_projects        → 2 PASS
Step 6: sync_vault           → 2 PASS
Step 7: batch_write_notes    → 6 PASS
Step 8: update_todo          → 6 PASS
=== Result: 26 passed, 0 failed ===
```

---

## Phase 16–17 完成項目（同日）

### Phase 16
- `delete_note` MCP tool（VaultIndexer + VaultService + server v3.5）
- `get_project_status` MCP tool（status.md 結構化讀取）
- E2E Steps 9–11（check_integrity / get_project_status / delete_note）
- 全規則合併 `_global/rules/`（01–14），清除 org 舊規則 22 個孤立向量
- `vault-coding-rules.instructions.md` 重構為 `search_vault(type:rule)` 動態發現

### Phase 17
- `AppConfig.vscode_user_path` 新增欄位
- `SetupService` Step 6：自動生成 `vault-coding-rules.instructions.md` + `VaultWriteConventions.instructions.md`
- E2E Step 12（vscode integration 冪等驗證）→ 46/46 PASS

---

## Phase 18 完成項目

### APScheduler 自動觸發（Item 1）
- 新增 `services/auto_scheduler.py`
  - `BackgroundScheduler`，3 個 cron job：
    - Weekly Mon 08:00 → `generate_weekly_summary()`
    - Monthly 1st 08:00 → `generate_monthly_summary()`
    - Daily 22:00 → `generate_daily_summary()`
  - `start()` / `stop()` / `block()` / `job_count()` 四個公開方法
- `main.py --scheduler` 守護模式入口
- `apscheduler>=3.10` 加入 `requirements.txt`
- E2E Step 13（6 checks：instantiate / job_count==3 / 3×manual trigger / stop）

### Daily note AI 彙整（Item 2）
- `generate_daily_summary()` 加入 auto-digest 邏輯：
  - `iProjects=None` 時自動呼叫 `_scan_today_conversations(date)` 填充
  - session 名稱從 conversations/ 檔名萃取（`{date}_{session}.md`）
- `_scan_today_conversations(iDate)` 新增（薄封裝 `_scan_all_project_conversations`）
- `_render_daily_summary_template()` 新增「今日 AI 對話」區塊（含連結清單）

### Bug Fix
- `auto_tasks.ps1`：修正 3 個已更名方法
  - `generate_daily/weekly/monthly_review(workspace)` → `generate_daily/weekly/monthly_summary()`

### 最終 E2E 結果
```
Step 13: AutoScheduler
  [PASS] AutoScheduler instantiates
  [PASS] job_count == 3 after start
  [PASS] _run_weekly() executes without raising
  [PASS] _run_monthly() executes without raising
  [PASS] _run_daily() executes without raising
  [PASS] stop() does not raise
=== Result: 52 passed, 0 failed ===
```

## 今日架構決策

| 決策 | 結論 |
|------|------|
| 兩套排程系統整合？ | 不整合；`.bat` 為主（Windows），`--scheduler` 為跨平台備用 |
| APScheduler 重複功能？ | 可接受；`generate_*_summary()` 永遠覆寫，後觸發蓋前一次 |
| `workspace` 參數移除？ | 是，所有 summary 方法現已自動掃描所有 org，不需指定 |
