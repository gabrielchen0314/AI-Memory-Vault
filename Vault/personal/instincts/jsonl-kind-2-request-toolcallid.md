---
id: "jsonl-kind-2-request-toolcallid"
trigger: "JSONL 的 kind=2 是每個 request 完成後的增量快照，需掃描所有行並以 toolCallId 去重才能取得完整 session 記錄"
confidence: 0.6
domain: "ai-memory-vault"
source: "auto-learn:LIFEOFDEVELOPMENT/ai-memory-vault"
created: "2026-04-12"
sequence: 60
---

# 學習：JSONL 的 kind=2 是每個 request 完成後的增量快照，需掃描所有行並以 toolC

## 動作
JSONL 的 kind=2 是每個 request 完成後的增量快照，需掃描所有行並以 toolCallId 去重才能取得完整 session 記錄
